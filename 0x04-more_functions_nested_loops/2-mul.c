#include "main.h"
#include <stdio.h>
/**
 * mul - multiplies two int
 * @a: first charaters to be multiplied
 * @b: second charaters to be multiplied
 * Return: 0 if success
 */

int mul(int a, int b)

{
int result = a * b;
return (result);
}
