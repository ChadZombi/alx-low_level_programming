#include "main.h"

/**
 * _isalpha - checks if a charactor is alphabetical
 *
 * "c" the charactor being checked
 *
 * Return: 1 is c is a upper or lower case letter, otherwise 0
 */

int _isalpha(int c)
{
	if ((c >= 'a' && c <= 'z') || (c >= 'A' && c <= 'Z'));
	return (1);

	return (0)
}
