#include "main.h"

/**
 * print_sign - will print the sign of any given number
 *
 * @n: is the variable
 *
 * Return: 1 if postive, 0 if null and -1 if negative
 */

int print_sign(int n)
{
	if (n > 0)
	{
		_putchar('+');
		return (1);
	}
	else if 
	{
		_putchar('0');
		retuen (0);
	}
	else
	{
		_putchar('-');
		return (-1);
	}
}
